---
- name: "Copy BashRC"
  copy:
    src: "{{ role_path }}/files/.bashrc"
    dest: "{{ ansible_env.HOME }}"
- name: "Read current mate terminal profiles"
  dconf:
    key: "/org/mate/terminal/global/profile-list"
    state: "read"
  register: "profile_list"
- name: "Adding our profile"
  set_fact:
    new_profile_list: "{{ profile_list.value | regex_replace(']$', \", 'pentest']\") }}"
- name: "Writing updated profile list"
  dconf:
    key: "/org/mate/terminal/global/profile-list"
    value: "{{ new_profile_list  }}"
  when: "'pentest' not in profile_list.value"
- name: "Restoring PenTest profile from Dump"
  shell:
    cmd: "dconf load /org/mate/terminal/profiles/pentest/ < {{ role_path }}/files/dconf-dump-pentest"
  when: "'pentest' not in profile_list.value"

